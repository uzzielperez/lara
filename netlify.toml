[build]
  command = "npm ci && npx prisma db push --force-reset && npm run db:generate && npm run build"
  publish = ".next"

[[plugins]]
  package = "@netlify/plugin-nextjs"

[build.environment]
  NODE_VERSION = "20.19.5"
  # Note: DATABASE_URL and GROQ_API_KEY must be set in Netlify UI
  # Go to Site settings → Environment variables
  # See NETLIFY_SETUP.md for details

